"""A2A client wiring. Generated by SDLC Accelerators (agentic-ai-adk domain skill).
One RemoteA2aAgent per a2a_agent tool binding.
"""
from google.adk.agents.remote_a2a_agent import RemoteA2aAgent

_A2A = {
{% for tb in tool_bindings if tb.type == "a2a_agent" %}    "{{ tb.name }}": RemoteA2aAgent(
        name="{{ tb.name }}",
        agent_card="{{ tb.endpoint }}",
        # auth: {{ tb.auth_method }}
    ),
{% endfor %}}


def get_a2a_agent(name: str):
    return _A2A[name]
