# /accelerator.ingest-epic (Brownfield) — RENAMED

This command was renamed to **/accelerator.epic-to-spec** and redesigned to fuse the Rally Epic with
the upstream CSA architecture.md and emit the final spec.md directly (no /speckit.specify step).

See `accelerator.epic-to-spec.md` in this directory.
