# /accelerator.assess

> The executable spec-kit command definition lives at `.specify/commands/accelerator.assess.md`.
> That is the file the coding agent loads when the developer types `/accelerator.assess`.
> This copy is a pointer to avoid drift — edit the `.specify/` version.
