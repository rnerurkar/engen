# /accelerator.ingest-epic

> The executable spec-kit command definition lives at `.specify/commands/accelerator.ingest-epic.md`.
> That is the file the coding agent loads when the developer types `/accelerator.ingest-epic`.
> This copy is a pointer to avoid drift — edit the `.specify/` version.
>
> Optional Greenfield front door: Rally Epic → signal-bearing `spec.md`. See Architecture
> § "Epic-to-Spec Ingestion (Greenfield)", Developer Guide § 2.3a, Operations Runbook § 9a.
