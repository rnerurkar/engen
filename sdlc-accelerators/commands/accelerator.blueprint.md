# /accelerator.blueprint

> The executable spec-kit command definition lives at `.specify/commands/accelerator.blueprint.md`.
> That is the file the coding agent loads when the developer types `/accelerator.blueprint`.
> This copy is a pointer to avoid drift — edit the `.specify/` version.
