# /accelerator.generate

> The executable spec-kit command definition lives at `.specify/commands/accelerator.generate.md`.
> That is the file the coding agent loads when the developer types `/accelerator.generate`.
> This copy is a pointer to avoid drift — edit the `.specify/` version.
